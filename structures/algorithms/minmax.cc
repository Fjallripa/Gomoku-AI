// Implementation of the minmax() algorithm
// ---------------------------------------------


// Min-Max algorithm
Square Computer::minmax () {
    int x = -1; int y = -1;
    
    //* The algorithm is not yet implemented. This is just the external structure set up already.

    return Square(this->board, x, y);
}
